# 全組み込み語一覧

このページは、現在の処理系が公開する組み込み語を機能グループ別に網羅した索引です。
スタック効果では左側が入力、右側が出力です。`T`、`E`、`N`などは辞書内の型変数・型制約であり、
利用者定義単語の宣言には書けません。失敗条件、資源上限、具体化規則は
[カテゴリ別の説明](builtins.md)と、そこからリンクされた詳細仕様を参照してください。

<!-- この表は tools/generate_builtin_reference.py で生成します。直接編集しないでください。 -->

## 基本（`CORE`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `足す` | `（N N -- N）` | 同じ型の2数値を加えます。 |
| `引く` | `（N N -- N）` | 同じ型の第1数値から第2数値を引きます。 |
| `掛ける` | `（N N -- N）` | 同じ型の2数値を掛けます。 |
| `等しい` | `（T T -- 真偽）` | 同じ型の2値が等しいかを調べます。 |
| `表示する` | `（表示可能 --）` | 値を改行せず表示します。 |
| `一行表示する` | `（表示可能 --）` | 値を表示して改行します。 |
| `改行する` | `（--）` | LFを1つ出力します。 |

## 制御フロー（`FLOW`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `比べて小さい` | `（N N -- 真偽）` | 同じ型の第1数値が第2数値より小さいかを調べます。 |

## 一次元配列（`ARRAY`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `空の整数配列` | `（-- 配列<整数>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の真偽配列` | `（-- 配列<真偽>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の文字配列` | `（-- 配列<文字>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の文字列配列` | `（-- 配列<文字列>）` | 要素型が確定した空の不変配列を作ります。 |
| `配列の長さ` | `（配列<T> -- 整数）` | 配列の要素数を返します。 |
| `配列から取り出す` | `（配列<T> 整数 -- T）` | 指定した添字の要素を返します。 |
| `配列の一部を取り出す` | `（配列<T> 整数 整数 -- 配列<T>）` | 半開区間の要素を新しい配列として返します。 |
| `配列の要素を置き換える` | `（配列<T> 整数 T -- 配列<T>）` | 指定した要素を置き換えた新しい配列を返します。 |
| `配列の末尾へ追加する` | `（配列<T> T -- 配列<T>）` | 末尾へ要素を追加した新しい配列を返します。 |
| `配列をつなぐ` | `（配列<T> 配列<T> -- 配列<T>）` | 第1配列の後ろへ第2配列の要素を順に置いた新しい配列を返します。 |
| `配列の先頭へ追加する` | `（配列<T> T -- 配列<T>）` | 先頭へ要素を追加した新しい配列を返します。 |
| `配列を逆順にする` | `（配列<T> -- 配列<T>）` | 要素を逆順に並べた新しい配列を返します。 |
| `配列に含まれる` | `（配列<T> T -- 真偽）` | 等しい要素が含まれるかを調べます。 |
| `配列から検索する` | `（配列<T> T -- 整数）` | 最初に等しい要素の位置を返し、見つからなければ-1を返します。 |
| `配列が空である` | `（配列<T> -- 真偽）` | 配列の要素数が0かを調べます。 |
| `配列の先頭を任意で取り出す` | `（配列<T> -- 任意<T>）` | 先頭要素を値ありで返し、空配列なら値なしを返します。 |
| `配列の末尾を任意で取り出す` | `（配列<T> -- 任意<T>）` | 末尾要素を値ありで返し、空配列なら値なしを返します。 |
| `配列の先頭を削除する` | `（配列<T> -- 配列<T>）` | 先頭要素を除いた新しい配列を返し、空配列はそのまま返します。 |
| `配列の末尾を削除する` | `（配列<T> -- 配列<T>）` | 末尾要素を除いた新しい配列を返し、空配列はそのまま返します。 |
| `配列の一部を削除する` | `（配列<T> 整数 整数 -- 配列<T>）` | 指定した半開区間を除いた新しい配列を返します。 |

## 数値（`NUM`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `割った商` | `（整数 整数 -- 整数）` | 整数を床除算した商を返します。 |
| `割った剰余` | `（整数 整数 -- 整数）` | 整数を床除算した剰余を返します。 |
| `割った商と剰余` | `（整数 整数 -- 整数 整数）` | 整数を床除算した商と剰余を返します。 |
| `小数で割る` | `（数値 数値 -- 小数）` | 2数値を既定精度で小数除算します。 |
| `精度指定で割る` | `（数値 数値 整数 丸め方法 -- 小数）` | 精度と丸め方法を指定して2数値を小数除算します。 |
| `絶対値` | `（N -- N）` | 数値の絶対値を返します。 |
| `最小値` | `（N N -- N）` | 同じ型の2数値の小さい方を返します。 |
| `最大値` | `（N N -- N）` | 同じ型の2数値の大きい方を返します。 |
| `小数に変換する` | `（整数 -- 小数）` | 整数を正確な小数へ変換します。 |
| `整数に変換する` | `（小数 -- 整数）` | 小数部が0の小数を整数へ変換します。 |
| `整数に丸める` | `（小数 丸め方法 -- 整数）` | 指定方法で小数を整数へ丸めます。 |
| `最近接偶数丸め` | `（-- 丸め方法）` | 丸め方法を表す組み込み値です。 |
| `四捨五入` | `（-- 丸め方法）` | 丸め方法を表す組み込み値です。 |
| `0方向へ丸め` | `（-- 丸め方法）` | 丸め方法を表す組み込み値です。 |
| `正方向へ丸め` | `（-- 丸め方法）` | 丸め方法を表す組み込み値です。 |
| `負方向へ丸め` | `（-- 丸め方法）` | 丸め方法を表す組み込み値です。 |
| `空の小数配列` | `（-- 配列<小数>）` | 要素型が確定した空の不変配列を作ります。 |

## 文字列と正規表現（`TEXT`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `つなぐ` | `（文字列 文字列 -- 文字列）` | 2文字列を順に連結します。 |
| `文字列を比較する` | `（文字列 文字列 -- 整数）` | 2文字列をUnicodeスカラー値順で比較します。 |
| `前後の空白を除く` | `（文字列 -- 文字列）` | 文字列の前後から規定の空白を除きます。 |
| `文字列の長さ` | `（文字列 -- 整数）` | 文字列の拡張書記素クラスタ数を返します。 |
| `文字列から取り出す` | `（文字列 整数 -- 文字）` | 書記素位置で文字を1つ取り出します。 |
| `文字列の一部を取り出す` | `（文字列 整数 整数 -- 文字列）` | 書記素の半開区間を新しい文字列として返します。 |
| `文字列から探す` | `（文字列 文字列 -- 整数）` | 書記素位置で部分文字列を探します。 |
| `文字列を置き換える` | `（文字列 文字列 文字列 -- 文字列）` | 一致する通常文字列をすべて置き換えます。 |
| `文字列を分割する` | `（文字列 文字列 -- 配列<文字列>）` | 通常文字列の区切りで文字列を分割します。 |
| `文字列のコードポイント数` | `（文字列 -- 整数）` | 文字列のUnicodeコードポイント数を返します。 |
| `文字列からコードポイントを取り出す` | `（文字列 整数 -- 文字）` | コードポイント位置で文字を1つ取り出します。 |
| `コードポイント範囲で文字列の一部を取り出す` | `（文字列 整数 整数 -- 文字列）` | コードポイントの半開区間を新しい文字列として返します。 |
| `コードポイント位置で文字列から探す` | `（文字列 文字列 -- 整数）` | コードポイント位置で部分文字列を探します。 |
| `整数を文字列に変換する` | `（整数 -- 文字列）` | 整数を正規10進表記の文字列へ変換します。 |
| `小数を文字列に変換する` | `（小数 -- 文字列）` | 小数を正規固定小数点表記の文字列へ変換します。 |
| `文字列を整数に変換する` | `（文字列 -- 整数）` | 正規10進表記の文字列を整数へ変換します。 |
| `文字列を小数に変換する` | `（文字列 -- 小数）` | 正規小数表記の文字列を小数へ変換します。 |
| `正規表現に完全一致する` | `（文字列 正規表現 -- 真偽）` | 文字列全体が正規表現に一致するかを返します。 |
| `正規表現を含む` | `（文字列 正規表現 -- 真偽）` | 正規表現に一致する部分があるかを返します。 |
| `正規表現で最初を取り出す` | `（文字列 正規表現 -- 文字列）` | 正規表現に最初に一致した部分文字列を返します。 |
| `正規表現の名前付き部分を取り出す` | `（文字列 正規表現 文字列 -- 文字列）` | 最初の一致から指定した名前付き部分を返します。 |
| `正規表現で置き換える` | `（文字列 正規表現 文字列 -- 文字列）` | 正規表現に一致する部分をテンプレートで置き換えます。 |
| `正規表現で分割する` | `（文字列 正規表現 -- 配列<文字列>）` | 正規表現の一致を区切りとして文字列を分割します。 |
| `大文字に変換する` | `（文字列 -- 文字列）` | Unicode 16.0の既定完全大文字写像を適用します。 |
| `小文字に変換する` | `（文字列 -- 文字列）` | Unicode 16.0の既定完全小文字写像を適用します。 |
| `大小文字を無視して比較する` | `（文字列 文字列 -- 整数）` | Unicode 16.0の既定完全ケースフォールド後に比較します。 |

## コンソールと実行情報（`IO`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `一行を入力する` | `（-- 入力結果）` | 標準入力から一行、終端、取消のいずれかを取得します。 |
| `入力行である` | `（入力結果 -- 入力結果 真偽）` | 入力結果が完成した行かを調べ、元の入力結果も残します。 |
| `入力終端である` | `（入力結果 -- 入力結果 真偽）` | 入力結果が入力終端かを調べ、元の入力結果も残します。 |
| `入力キャンセルである` | `（入力結果 -- 入力結果 真偽）` | 入力結果が取消かを調べ、元の入力結果も残します。 |
| `入力行を取り出す` | `（入力結果 -- 文字列）` | 行状態の入力結果から文字列を取り出します。 |
| `入力結果を捨てる` | `（入力結果 --）` | 入力結果を状態にかかわらず破棄します。 |
| `エラー表示する` | `（表示可能 --）` | 値を標準エラーへ表示します。 |
| `エラー一行表示する` | `（表示可能 --）` | 値を標準エラーへ表示してLFを出力します。 |
| `エラー改行する` | `（--）` | 標準エラーへLFを1つ出力します。 |
| `終了する` | `（整数 --）` | 指定した0から255の終了コードでプログラムを終了します。 |
| `起動引数を得る` | `（-- 配列<文字列>）` | プログラムへ渡された起動引数を文字列配列で返します。 |
| `プログラム名を得る` | `（-- 文字列）` | 実行中プログラムの論理名を返します。 |
| `プログラムの場所を得る` | `（-- 文字列）` | 実行中プログラムの場所をURI文字列で返します。 |
| `待つ` | `（整数 --）` | 指定したミリ秒だけ実行環境へ待機を要求します。 |
| `単調ミリ秒を得る` | `（-- 整数）` | 実行開始からの非減少な経過ミリ秒を返します。 |
| `現在日時を得る` | `（-- 日時）` | 実行環境の現在日時とUTCオフセットを取得します。 |
| `日時を文字列に変換する` | `（日時 -- 文字列）` | 日時をロケール非依存の固定形式へ変換します。 |

## JSON（`JSON`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `JSONヌル` | `（-- JSON）` | JSONのnull値を作ります。 |
| `空のJSON配列` | `（-- JSON）` | 空の不変JSON配列を作ります。 |
| `空のJSONオブジェクト` | `（-- JSON）` | 空の不変JSONオブジェクトを作ります。 |
| `JSONを解析する` | `（文字列 -- JSON）` | 文字列全体を厳密なJSONとして解析します。 |
| `JSONを文字列に変換する` | `（JSON -- 文字列）` | JSON値を決定的なコンパクトJSONへ変換します。 |
| `真偽をJSONに変換する` | `（真偽 -- JSON）` | 真偽をJSON真偽へ変換します。 |
| `整数をJSONに変換する` | `（整数 -- JSON）` | 整数を同じ値のJSON整数へ変換します。 |
| `小数をJSONに変換する` | `（小数 -- JSON）` | 小数を同じ値のJSON小数へ変換します。 |
| `文字列をJSONに変換する` | `（文字列 -- JSON）` | 文字列を同じ値のJSON文字列へ変換します。 |
| `JSONから真偽を取り出す` | `（JSON -- 真偽）` | JSON真偽からBSB真偽を取り出します。 |
| `JSONから整数を取り出す` | `（JSON -- 整数）` | JSON整数からBSB整数を取り出します。 |
| `JSONから小数を取り出す` | `（JSON -- 小数）` | JSON小数からBSB小数を取り出します。 |
| `JSONから文字列を取り出す` | `（JSON -- 文字列）` | JSON文字列からBSB文字列を取り出します。 |
| `JSONヌルである` | `（JSON -- JSON 真偽）` | JSON値がnullかを調べ、元値も残します。 |
| `JSON真偽である` | `（JSON -- JSON 真偽）` | JSON値が真偽かを調べ、元値も残します。 |
| `JSON整数である` | `（JSON -- JSON 真偽）` | JSON値が整数かを調べ、元値も残します。 |
| `JSON小数である` | `（JSON -- JSON 真偽）` | JSON値が小数かを調べ、元値も残します。 |
| `JSON文字列である` | `（JSON -- JSON 真偽）` | JSON値が文字列かを調べ、元値も残します。 |
| `JSON配列である` | `（JSON -- JSON 真偽）` | JSON値が配列かを調べ、元値も残します。 |
| `JSONオブジェクトである` | `（JSON -- JSON 真偽）` | JSON値がオブジェクトかを調べ、元値も残します。 |
| `JSONから配列に変換する` | `（JSON -- 配列<JSON>）` | JSON配列をBSBのJSON値配列へ変換します。 |
| `JSON配列に変換する` | `（配列<JSON> -- JSON）` | BSBのJSON値配列をJSON配列へ変換します。 |
| `JSON配列の長さ` | `（JSON -- 整数）` | JSON配列の要素数を返します。 |
| `JSON配列から取り出す` | `（JSON 整数 -- JSON）` | JSON配列の指定位置から要素を取り出します。 |
| `JSON配列の一部を取り出す` | `（JSON 整数 整数 -- JSON）` | JSON配列の半開区間を新しいJSON配列で返します。 |
| `JSON配列の要素を置き換える` | `（JSON 整数 JSON -- JSON）` | 指定要素を置換した新しいJSON配列を返します。 |
| `JSON配列の末尾へ追加する` | `（JSON JSON -- JSON）` | 末尾へ要素を加えた新しいJSON配列を返します。 |
| `JSONオブジェクトの要素数` | `（JSON -- 整数）` | JSONオブジェクトのメンバー数を返します。 |
| `JSONオブジェクトのキー一覧` | `（JSON -- 配列<文字列>）` | JSONオブジェクトのキーを保持順で返します。 |
| `JSONオブジェクトにキーがある` | `（JSON 文字列 -- JSON 真偽）` | キーの存在を調べ、元オブジェクトも残します。 |
| `JSONオブジェクトから必須値を取り出す` | `（JSON 文字列 -- JSON）` | 存在するキーのJSON値を取り出します。 |
| `JSONオブジェクトに設定する` | `（JSON 文字列 JSON -- JSON）` | キーを設定した新しいJSONオブジェクトを返します。 |
| `JSONオブジェクトから削除する` | `（JSON 文字列 -- JSON）` | キーを削除した新しいJSONオブジェクトを返します。 |

## 任意値（`OPT`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `任意にする` | `（T -- 任意<T>）` | 値を値あり任意値へ包みます。 |
| `任意に値がある` | `（任意<T> -- 任意<T> 真偽）` | 任意値を残して値があるかを調べます。 |
| `任意から値を取り出す` | `（任意<T> -- T）` | 値あり任意値から内包値を取り出します。 |
| `任意を捨てる` | `（任意<T> --）` | 任意値を状態にかかわらず破棄します。 |
| `JSONオブジェクトから任意値を取り出す` | `（JSON 文字列 -- 任意<JSON>）` | キーがあれば値あり、なければ値なしの任意JSONを返します。 |

## 結果値（`RESULT`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `成功にする` | `（T -- 結果<T,E>）` | 値を成功結果へ包みます。 |
| `失敗にする` | `（E -- 結果<T,E>）` | 値を失敗結果へ包みます。 |
| `結果が成功である` | `（結果<T,E> -- 結果<T,E> 真偽）` | 結果値を残して成功状態かを調べます。 |
| `結果が失敗である` | `（結果<T,E> -- 結果<T,E> 真偽）` | 結果値を残して失敗状態かを調べます。 |
| `結果から成功値を取り出す` | `（結果<T,E> -- T）` | 成功結果から成功値を取り出します。 |
| `結果から失敗値を取り出す` | `（結果<T,E> -- E）` | 失敗結果から失敗値を取り出します。 |
| `結果を捨てる` | `（結果<T,E> --）` | 結果値を状態にかかわらず破棄します。 |

## 回復可能なJSON解析（`RJSON`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `JSONを解析して結果を返す` | `（文字列 -- 結果<JSON,JSON解析失敗>）` | 文字列全体を厳密なJSONとして解析し、構文失敗を結果値で返します。 |
| `JSON解析失敗の種類を取り出す` | `（JSON解析失敗 -- 文字列）` | JSON解析失敗から安定した失敗種別を取り出します。 |
| `JSON解析失敗のバイト位置を取り出す` | `（JSON解析失敗 -- 整数）` | JSON解析失敗から0始まりのUTF-8バイト位置を取り出します。 |
| `JSON解析失敗の行を取り出す` | `（JSON解析失敗 -- 整数）` | JSON解析失敗から1始まりの行番号を取り出します。 |
| `JSON解析失敗の列を取り出す` | `（JSON解析失敗 -- 整数）` | JSON解析失敗から1始まりのUnicodeスカラー値列を取り出します。 |

## JSON形状検証（`JSHAPE`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `JSONヌルの形状` | `（-- JSON形状）` | JSON nullだけを受理する形状を作ります。 |
| `JSON真偽の形状` | `（-- JSON形状）` | JSON真偽だけを受理する形状を作ります。 |
| `JSON整数の形状` | `（-- JSON形状）` | JSON整数だけを受理する形状を作ります。 |
| `JSON小数の形状` | `（-- JSON形状）` | JSON小数だけを受理する形状を作ります。 |
| `JSON文字列の形状` | `（-- JSON形状）` | JSON文字列だけを受理する形状を作ります。 |
| `JSON配列の形状にする` | `（JSON形状 -- JSON形状）` | 形状を全要素へ適用するJSON配列形状を作ります。 |
| `空のJSONオブジェクト形状` | `（-- JSON形状）` | メンバー制約のないJSONオブジェクト形状を作ります。 |
| `JSON形状に必須キーを設定する` | `（JSON形状 文字列 JSON形状 -- JSON形状）` | JSONオブジェクト形状へ必須キーを設定します。 |
| `JSON形状に任意キーを設定する` | `（JSON形状 文字列 JSON形状 -- JSON形状）` | JSONオブジェクト形状へ任意キーを設定します。 |
| `JSON形状をヌル許容にする` | `（JSON形状 -- JSON形状）` | JSON nullまたは元の形状を受理する形状を作ります。 |
| `JSONの形状を検証する` | `（JSON JSON形状 -- 結果<JSON,配列<JSON形状失敗>>）` | JSON値を形状で検証し、原値または閉じた失敗配列を返します。 |
| `JSON形状失敗の種類を取り出す` | `（JSON形状失敗 -- 文字列）` | JSON形状失敗から閉じた種類を取り出します。 |
| `JSON形状失敗のパスを取り出す` | `（JSON形状失敗 -- 文字列）` | JSON形状失敗からJSON Pointer形式のパスを取り出します。 |
| `JSON形状失敗の期待種類を取り出す` | `（JSON形状失敗 -- 文字列）` | JSON形状失敗から期待したJSON種類を取り出します。 |
| `JSON形状失敗の実際種類を取り出す` | `（JSON形状失敗 -- 文字列）` | JSON形状失敗から実際のJSON種類を取り出します。 |

## 論理接続（`CONN`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `論理接続を確認する` | `（--）` | ホスト側に論理接続の設定が存在し、利用可能かを確認します。 |

## バイト列（`BYTES`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `空のバイト列` | `（-- バイト列）` | 空の不変バイト列を返します。 |
| `バイト列の長さ` | `（バイト列 -- 整数）` | バイト列の論理バイト数を返します。 |
| `バイト列の一部を取り出す` | `（バイト列 整数 整数 -- バイト列）` | バイト半開区間を元値を変えずに返します。 |
| `文字列をUTF8バイト列に変換する` | `（文字列 -- バイト列）` | 文字列をBOMなしの最短UTF-8へ符号化します。 |
| `バイト列をUTF8文字列に変換して結果を返す` | `（バイト列 -- 結果<文字列,UTF8復号失敗>）` | バイト列を厳密なUTF-8として復号し結果値で返します。 |
| `UTF8復号失敗の種類を取り出す` | `（UTF8復号失敗 -- 文字列）` | UTF-8復号失敗から安定した失敗種別を取り出します。 |
| `UTF8復号失敗のバイト位置を取り出す` | `（UTF8復号失敗 -- 整数）` | UTF-8復号失敗から0始まりのバイト位置を取り出します。 |
| `バイト列をBase64文字列に変換する` | `（バイト列 -- 文字列）` | バイト列をRFC 4648の規範Base64文字列へ符号化します。 |
| `Base64文字列をバイト列に変換して結果を返す` | `（文字列 -- 結果<バイト列,Base64復号失敗>）` | 文字列を厳密な規範Base64として復号し結果値で返します。 |
| `Base64復号失敗の種類を取り出す` | `（Base64復号失敗 -- 文字列）` | Base64復号失敗から安定した失敗種別を取り出します。 |
| `Base64復号失敗の文字位置を取り出す` | `（Base64復号失敗 -- 整数）` | Base64復号失敗から0始まりのUnicodeスカラー値位置を取り出します。 |

## HTTPS（`HTTPS`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `空のHTTP要求` | `（-- HTTP要求）` | 経路・問い合わせ・ヘッダーが空で本文不在の不変HTTP要求を返します。 |
| `HTTP要求に経路を設定する` | `（HTTP要求 文字列 -- HTTP要求）` | 相対Unicode経路を設定した新しいHTTP要求を返します。 |
| `HTTP要求に問い合わせ項目を追加する` | `（HTTP要求 文字列 文字列 -- HTTP要求）` | 問い合わせ名と値を順序と重複を保って追加します。 |
| `HTTP要求にヘッダーを設定する` | `（HTTP要求 文字列 文字列 -- HTTP要求）` | ASCIIヘッダーを大小文字非区別で置換します。 |
| `HTTP要求にJSON本文を設定する` | `（HTTP要求 JSON -- HTTP要求）` | 規範JSONを設定時にUTF-8本文へ固定します。 |
| `HTTP要求に文字列本文を設定する` | `（HTTP要求 文字列 -- HTTP要求）` | 文字列を設定時にUTF-8本文へ固定します。 |
| `HTTP要求にバイト列本文を設定する` | `（HTTP要求 バイト列 -- HTTP要求）` | 不変バイト列を損失なく本文へ設定します。 |
| `HTTP要求を送信する` | `（HTTP要求 -- 結果<HTTP応答,HTTP送信失敗>）` | 静的な論理接続とmethodを使ってHTTP要求を同期送信します。 |
| `HTTP応答から状態コードを取り出す` | `（HTTP応答 -- 整数）` | 完全HTTP応答から最終状態コードを取り出します。 |
| `HTTP応答からヘッダー値一覧を取り出す` | `（HTTP応答 文字列 -- 配列<文字列>）` | HTTP応答から同名ヘッダーの全値を受信順で取り出します。 |
| `HTTP応答から本文を取り出す` | `（HTTP応答 -- バイト列）` | HTTP応答から完全受信済み本文を取り出します。 |
| `HTTP送信失敗の種類を取り出す` | `（HTTP送信失敗 -- 文字列）` | HTTP送信失敗から安定した閉じた種類を取り出します。 |
| `HTTP要求に本文がある` | `（HTTP要求 -- HTTP要求 真偽）` | HTTP要求を残して長さ0を含む本文の有無を返します。 |

## HTTP API相互運用（`HTTP-API`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `文字列表をフォームURL符号化する` | `（配列<配列<文字列>> -- 文字列）` | 名前と値の文字列二次元配列をapplication/x-www-form-urlencoded文字列へ変換します。 |
| `HTTP応答を成功状態として検査する` | `（HTTP応答 -- 結果<HTTP応答,HTTP応答>）` | 2xx応答を成功、それ以外の最終応答を本文付きの失敗として結果へ変換します。 |

## 二次元配列（`NARRAY`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `空の整数二次元配列` | `（-- 配列<配列<整数>>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の真偽二次元配列` | `（-- 配列<配列<真偽>>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の文字二次元配列` | `（-- 配列<配列<文字>>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の文字列二次元配列` | `（-- 配列<配列<文字列>>）` | 要素型が確定した空の不変配列を作ります。 |
| `空の小数二次元配列` | `（-- 配列<配列<小数>>）` | 要素型が確定した空の不変配列を作ります。 |
| `空のJSON二次元配列` | `（-- 配列<配列<JSON>>）` | 要素型が確定した空の不変配列を作ります。 |

## 作業領域、ファイル、表（`WST`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `ファイルを読む` | `（文字列 -- 結果<バイト列,ファイル読取失敗>）` | 静的作業領域の登録から論理ファイル全体を読みます。 |
| `ファイルへ書く` | `（文字列 バイト列 -- 結果<整数,ファイル書込失敗>）` | 静的作業領域の登録へ全バイトを原子的に書きます。 |
| `ファイル読取失敗の種類を取り出す` | `（ファイル読取失敗 -- 文字列）` | ファイル読取失敗から安定した閉じた種類を取り出します。 |
| `ファイル書込失敗の種類を取り出す` | `（ファイル書込失敗 -- 文字列）` | ファイル書込失敗から安定した閉じた種類を取り出します。 |
| `CSVを表として解析する` | `（文字列 -- 結果<配列<配列<文字列>>,区切りテキスト解析失敗>）` | CSV文字列を厳密に解析し、文字列の二次元配列を結果で返します。 |
| `TSVを表として解析する` | `（文字列 -- 結果<配列<配列<文字列>>,区切りテキスト解析失敗>）` | TSV文字列を厳密に解析し、文字列の二次元配列を結果で返します。 |
| `表をCSVに変換する` | `（配列<配列<文字列>> -- 文字列）` | 文字列の二次元配列を規範CSV文字列へ変換します。 |
| `表をTSVに変換する` | `（配列<配列<文字列>> -- 文字列）` | 文字列の二次元配列を規範TSV文字列へ変換します。 |
| `区切りテキスト解析失敗の種類を取り出す` | `（区切りテキスト解析失敗 -- 文字列）` | 区切りテキスト解析失敗から安定した閉じた種類を取り出します。 |
| `区切りテキスト解析失敗のバイト位置を取り出す` | `（区切りテキスト解析失敗 -- 整数）` | 区切りテキスト解析失敗から0始まりのUTF-8バイト位置を取り出します。 |
| `区切りテキスト解析失敗の行を取り出す` | `（区切りテキスト解析失敗 -- 整数）` | 区切りテキスト解析失敗から1始まりの行を取り出します。 |
| `区切りテキスト解析失敗の列を取り出す` | `（区切りテキスト解析失敗 -- 整数）` | 区切りテキスト解析失敗から1始まりの列を取り出します。 |

## JSON参照・構築（`JERG`）

| 単語 | スタック効果 | 説明 |
|---|---|---|
| `JSONをポインターで任意参照する` | `（JSON 文字列 -- 任意<JSON>）` | RFC 6901 JSON Pointerで参照し、対象がなければ値なしを返します。 |
| `JSONオブジェクトを構築する` | `（配列<文字列> 配列<JSON> -- JSON）` | 同じ長さのキー配列とJSON値配列から不変オブジェクトを構築します。 |

## 更新方法

処理系の辞書を変更したときは、有効なソースに対する `explain --json` の出力を保存し、次を実行します。

```shell
python3 tools/generate_builtin_reference.py explain.json docs/reference/builtin-catalog.md
```

生成元となる `builtinWords` は、未使用の語も含む辞書順の完全一覧です。
