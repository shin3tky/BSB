# HTTP-REL independent conformance data

`catalog.tsv` fixes all 31 public IDs. `timing.tsv` is an implementation-independent table for
bounded backoff, Retry-After capping, and start-interval composition. No network or real clock is
used by these expectations.

