氏名は 定数 「\u{3000}山田 太郎\u{3000}」。
請求行は 定数 「請求番号: INV-2026-0042」。
請求形式は 定数 正規表現「INV-(?<year>[0-9]{4})-(?<number>[0-9]{4})」。

メインとは （--）
    氏名 を 前後の空白を除く
    「さん、こんにちは」 と つなぐ
    一行表示する
    氏名 を 前後の空白を除く
    「 」 で 文字列を分割する
    一行表示する
    請求行 と 請求形式 と 「year」 で 正規表現の名前付き部分を取り出す
    一行表示する
    請求行 と 請求形式 と 「number」 で 正規表現の名前付き部分を取り出す
    一行表示する
こと。
