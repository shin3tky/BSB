出力結果は 変数 「未変更」。
作業結果は 変数 「未変更」。

メインとは （--）
    内容は 変数 「x」。
    23 回だけ
        内容 と 内容 を つなぐ を 内容 に 入れる
    繰り返す
    JSON文字列は 定数 「\"」 と 内容 を つなぐ 「\"」 を つなぐ。
    値は 定数 JSON文字列 を JSONを解析する。
    6 回だけ
        値 を JSONを文字列に変換する を 作業結果 に 入れる
    繰り返す
    値 を JSONを文字列に変換する を 出力結果 に 入れる
こと。
